APP_PORT=8080
APP_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cms_db

# Secret untuk session CMS (ganti string acak panjang)
SESSION_SECRET=ganti-ini-dengan-string-rahasia-panjang

# Secret untuk JWT API (dipakai portal React)
JWT_SECRET=ganti-ini-juga-dengan-string-berbeda

# CORS: izinkan portal React akses API
ALLOWED_ORIGINS=http://localhost:5173
